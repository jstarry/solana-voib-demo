[workspace]
members = [
  "bandwidth-prepay-api",
  "bandwidth-tps",
  "client",
  "client-tester",
  "gatekeeper",
  "provider-drone",
  "tcp-echo-server",
  "stream-video",
]
exclude = [
  "bandwidth-prepay-program",
]
